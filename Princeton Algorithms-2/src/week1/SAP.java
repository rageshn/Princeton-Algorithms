/**
 * 
 */
package week1;
import edu.princeton.cs.algs4.Digraph;
/**
 * @author ragesh
 *
 */
public class SAP {
	
	/**
	 * constructor takes a digraph (not necessarily a DAG)
	 * @param Digraph
	 */
	public SAP(Digraph G) {
		
	}
	
	/**
	 * Calculates the length of the shortest ancestral path 
	 * @param v
	 * @param w
	 * @return length of shortest ancestral path between v and w; -1 if no such path
	 */
	public int length(int v, int w) {
		return 0;
	}
	
	/**
	 * Finds the common ancestor of v and w
	 * @param v
	 * @param w
	 * @return a common ancestor of v and w that participates in a shortest ancestral path; -1 if no such path
	 */
	public int ancestor(int v, int w) {
		return 0;
	}
	
	/**
	 * Finds the shortest ancestral path between any vertex in v and w. 
	 * @param v
	 * @param w
	 * @return length of shortest ancestral path between any vertex in v and any vertex in w; -1 if no such path
	 */
	public int length(Iterable<Integer> v, Iterable<Integer> w) {
		return 0;
	}
	
	/**
	 * Finds the common ancestor that participates in shortest ancestral path
	 * @param v
	 * @param w
	 * @return a common ancestor that participates in shortest ancestral path; -1 if no such path
	 */
	public int ancestor(Iterable<Integer> v, Iterable<Integer> w) {
		return 0;
	}
	
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
